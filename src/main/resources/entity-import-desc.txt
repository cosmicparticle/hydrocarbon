1、导入的数据以上传文件中的第1个Sheet为准，Sheet名不限；
2、表格中第二行为表头行，每个表头必须与下载的导入模板中的表头一致；
3、导入数据从第三行开始，每行的第一列为序号，必须有值填入；
4、表头包含”[数字]“的为多值属性字段或者关系字段，中括号内的数字相同代表是同一个多值属性/关系；
5、后缀为“$$label$$”的表头，代表关系的名称，如果该关系的其他属性已经填写了，那么这个属性必须要填写，否则将会导入失败；